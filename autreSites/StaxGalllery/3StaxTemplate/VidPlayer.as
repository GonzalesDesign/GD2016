package {	/*import flash.display.MovieClip;	import flash.display.DisplayObject;        import flash.events.*;*/		import flash.net.NetConnection;	import flash.net.NetStream;	import flash.media.Video;	import flash.display.Sprite;	import flash.display.DisplayObject;    public class VidPlayer extends Sprite {		//private var vid:Video = new Video();		//private var nc:NetConnection = new NetConnection();		//private var ns:NetStream = new NetStream(nc);       /* private var targetVidHolder:MovieClip;		private var playBtn:MovieClip;		private var videoURL:String;		private var nVidCount:Number = 4;		private var vidSelectBtn:MovieClip;		private var myVideoSelectBtn1:MovieClip;		private var aVideoSelectBtn1:Array = new Array();		//private var videoPlaying:Boolean = true;		private var toggleVideoPausePlay:Boolean;		private var video:Video = new Video(160, 120);        private var connection:NetConnection =  new NetConnection();        private var stream:NetStream;// = new NetStream(connection);*/        public function VidPlayer(nc:NetConnection, vid:Video, ns:NetStream, flick:String, xpos:int, ypos:int) {			nc = new NetConnection();			nc.connect(null);			ns = new NetStream(nc);			//ns = NetStream(nc);			vid = new Video();			vid.attachNetStream(ns);			ns.play(flick);			vid.x = xpos;			vid.y = ypos;			//removeChild(vid);			//vid.clear();			addChild(vid);									/*var target:DisplayObject = getChildByName("vid"); 			trace("getChildIndex(target): " + getChildIndex(target)); // 0*/        }	}}